import streamlit as st

st.markdown(
    """
### Why ML is different from classical Software engineering
- need to track the code **and the data** (training data, models, parameters, ...)
- the output of a training process is not **predictable**: it is exploratory work: need to track experiments
- need interactive UI to dig in model performance: test the model, show metrics graphs, ...
    """
)
